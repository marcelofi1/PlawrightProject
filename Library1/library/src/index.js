const { defaultButtonValidation } = require('./validations/defaultButtonValidation');
const { primaryButtonValidation } = require('./validations/primaryButtonValidation');

module.exports = {
  defaultButtonValidation,
  primaryButtonValidation
};